---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: stateful-1
spec:
  serviceName: "stateful-service"
  replicas: 2
  selector:
    matchLabels:
      app: movie
  template:
    metadata:
      labels:
        app: movie
    spec:
      containers:
        - name: cont-1
          image: nginx
          ports:
            - containerPort: 80
          volumeMounts:
            - name: flm
              mountPath: /tmp/cont1

        - name: cont-2
          image: httpd
          command: ["/bin/bash", "-c", "sleep 3600"]
          ports:
            - containerPort: 80
          volumeMounts:
            - name: flm
              mountPath: /tmp/cont2

      volumes:
        - name: flm
          persistentVolumeClaim:
            claimName: pvc

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  awsElasticBlockStore:
    volumeID: vol-030dfa5ed9bfb5e21
    fsType: ext4

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi